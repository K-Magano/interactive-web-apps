// const nickname = "Timmy"
// const firstname = "Timothy" 
// //try const user = nickname || (firstname) || 'good Morning
// if (nickname === "Timmy"){
// console.log (`Good Morning, ${nickname} !` )
    
// } else if (firstname === "Timothy")
// {
// console.log (`Good Morning, ${firstname} !` )
    
// } else(nickname && firstname === " ")
// {
//     console.log(`Good Morning`)
// }
// //console.log(`Good Morning, ${nickname} || ${firstname}!`) //logs 'Good Morning, Timmy ||Timothy!'

// //using interpolation because its a string, used template literals to indicate it's a string


const nickname =  ' '
const firstname = ' '  
const user = nickname || firstname 

{console.log(`Good Morning! ${user}`)}

if (user = ' ' )
    {console.log(`Good Morning!`)}
  
// Two ways, but second worked better because of || and fallback variables and throws and syntax //! error